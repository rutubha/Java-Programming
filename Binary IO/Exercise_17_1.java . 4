.add(input.nextLine());
                }
            }
        }

        //Generate Integer
        for(int i = 0; i < 100; i++) {
            list.add((int)(Math.random() * 100) + " ");
        }

  